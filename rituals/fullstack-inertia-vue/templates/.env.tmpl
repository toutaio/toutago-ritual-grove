# Server Configuration
PORT={{.port}}
DB_DRIVER={{.db_driver}}

# Database Configuration
{{if eq .db_driver "postgres"}}DB_HOST=localhost
DB_PORT=5432
DB_NAME={{.project_name}}
DB_USER=postgres
DB_PASSWORD=
DB_SSLMODE=disable{{else}}DB_HOST=localhost
DB_PORT=3306
DB_NAME={{.project_name}}
DB_USER=root
DB_PASSWORD={{end}}

# Application
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost:{{.port}}

{{if .use_ssr}}# SSR Configuration
SSR_ENABLED=true
SSR_URL=http://localhost:13714{{end}}
