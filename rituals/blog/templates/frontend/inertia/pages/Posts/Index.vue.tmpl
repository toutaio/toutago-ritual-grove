<script setup>
import { Link } from '@toutaio/inertia-vue'
import Layout from '../../components/Layout.vue'

defineProps({
  posts: Array,
  pagination: Object,
})
</script>

<template>
  <Layout>
    <div class="posts-index">
      <div class="header">
        <h1>All Posts</h1>
        <Link href="/posts/create" class="btn btn-primary">Create Post</Link>
      </div>

      <div v-if="posts && posts.length > 0" class="posts-list">
        <article v-for="post in posts" :key="post.id" class="post-item">
          <h2>
            <Link :href="`/posts/${post.id}`">{{"{{"}} post.title {{"}}"}}</Link>
          </h2>
          <p class="post-excerpt">{{"{{"}} post.excerpt {{"}}"}}</p>
          <div class="post-meta">
            <span class="post-author">By {{"{{"}} post.author {{"}}"}}</span>
            <span class="post-date">{{"{{"}} new Date(post.created_at).toLocaleDateString() {{"}}"}}</span>
            <span v-if="post.category" class="post-category">{{"{{"}} post.category {{"}}"}}</span>
          </div>
          <div class="post-actions">
            <Link :href="`/posts/${post.id}`" class="btn btn-sm">View</Link>
            <Link :href="`/posts/${post.id}/edit`" class="btn btn-sm btn-secondary">Edit</Link>
          </div>
        </article>
      </div>
      <p v-else>No posts found.</p>

      <nav v-if="pagination" class="pagination">
        <Link 
          v-if="pagination.prev_page" 
          :href="`/posts?page=${pagination.prev_page}`"
          class="btn"
        >
          Previous
        </Link>
        <span class="page-info">Page {{"{{"}} pagination.current_page {{"}}"}} of {{"{{"}} pagination.total_pages {{"}}"}}</span>
        <Link 
          v-if="pagination.next_page" 
          :href="`/posts?page=${pagination.next_page}`"
          class="btn"
        >
          Next
        </Link>
      </nav>
    </div>
  </Layout>
</template>

<style scoped>
.posts-index {
  max-width: 800px;
  margin: 0 auto;
  padding: 2rem;
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 2rem;
}

.posts-list {
  display: flex;
  flex-direction: column;
  gap: 2rem;
}

.post-item {
  border-bottom: 1px solid #e0e0e0;
  padding-bottom: 2rem;
}

.post-item:last-child {
  border-bottom: none;
}

.post-meta {
  display: flex;
  gap: 1rem;
  font-size: 0.875rem;
  color: #666;
  margin: 1rem 0;
}

.post-actions {
  display: flex;
  gap: 0.5rem;
}

.pagination {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 1rem;
  margin-top: 3rem;
}

.page-info {
  font-size: 0.875rem;
  color: #666;
}

.btn {
  padding: 0.5rem 1rem;
  border-radius: 4px;
  text-decoration: none;
  background: #007bff;
  color: white;
  border: none;
  cursor: pointer;
}

.btn-sm {
  padding: 0.25rem 0.75rem;
  font-size: 0.875rem;
}

.btn-secondary {
  background: #6c757d;
}

.btn-primary {
  background: #28a745;
}
</style>
