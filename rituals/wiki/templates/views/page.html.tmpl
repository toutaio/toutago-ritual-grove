<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{.Page.Title}} - {{.wiki_name}}</title>
    <link rel="stylesheet" href="/static/wiki.css">
</head>
<body>
    <header>
        <h1><a href="/">{{.wiki_name}}</a></h1>
        <nav>
            <a href="/new">New Page</a>
            <a href="/wiki/{{.Page.Slug}}/edit">Edit</a>
            <a href="/wiki/{{.Page.Slug}}/history">History</a>
{{if .enable_search}}
            <form action="/search" method="get" class="search-form">
                <input type="search" name="q" placeholder="Search..." />
                <button type="submit">Search</button>
            </form>
{{end}}
        </nav>
    </header>

    <main>
        <article>
            <h2>{{.Page.Title}}</h2>
            <div class="page-meta">
                Version {{.Page.Version}} | Last updated: {{.Page.UpdatedAt}}
            </div>
            <div class="page-content">
                {{.RenderedContent}}
            </div>
        </article>
    </main>

    <footer>
        <p>Powered by {{.wiki_name}}</p>
    </footer>
    <script src="/static/wiki.js"></script>
</body>
</html>
